Note: since `lemur` consumes the Etherscan API for processing incoming transactions, it won't work on a local blockchain.

## Run

```bash

ENVIRONMENT=testing MONGODB_URI=<uri> node ./lemur.js <event-name> [<treshold>]

```
